services:
  dns_checker:
    image: charly3012/dns-checker
    #build: .
    container_name: dns-checker
    restart: unless-stopped

    env_file:
      - .env

    # environment:
    #   - CHECK_INTERVAL=60 #Seconds
    #   - DOMAIN_TYPE=A
    #   - DNS_NAMESERVER=1.1.1.1 
    #   - DOMAINS= domain1.com, domain2.com, domain3.com
    #   - WEBHOOK_URL=

